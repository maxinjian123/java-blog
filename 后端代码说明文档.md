# 个人博客系统 - 后端代码说明文档

## 概述

基于 **Spring Boot 3.2.5 + MyBatis-Flex 1.11.8 + Redis + MinIO + Knife4j 4.4.0** 构建的高性能个人博客后端系统。采用标准三层架构（Controller → Service → Mapper），严格遵循阿里企业级Java开发规范，提供完整的文章发布、权限管理、热门排行、内容推荐、评论互动、SEO优化等博客核心能力。

---

## 技术栈

| 分类 | 技术选型 | 版本 | 说明 |
|------|---------|------|------|
| 核心框架 | Spring Boot | 3.2.5 | 基础Web框架，要求JDK 17+ |
| ORM | MyBatis-Flex | 1.11.8 | 增强型MyBatis，支持APT表定义生成、Lambda查询、分页 |
| 数据库 | MySQL | 9.3.x | 主存储，InnoDB引擎 |
| 连接池 | Druid | 1.2.23 | 阿里高性能连接池，带监控能力 |
| 缓存 | Spring Data Redis | 3.2.x | Redis ZSET做排行榜，Session做分布式登录 |
| 分布式会话 | Spring Session Data Redis | - | Session共享存储于Redis |
| 对象存储 | MinIO | 8.6.0 | 私有云存储封面/附件 |
| 接口文档 | Knife4j (OpenAPI 3) | 4.4.0 | 自动生成接口调试UI |
| 工具库 | Hutool | 5.8.40 | 通用Java工具类集合 |
| 参数校验 | Jakarta Validation | - | `@Valid` 注解驱动参数校验 |
| 简化代码 | Lombok | - | `@Data`/`@Builder` 消除样板代码 |

---

## 快速开始

### 环境要求

- JDK ≥ 17
- Maven ≥ 3.8.x
- MySQL ≥ 8.0
- Redis ≥ 6.0
- MinIO ≥ RELEASE.2023

### 1. 初始化数据库

```sql
-- 执行建表脚本
mysql -uroot -p < src/main/resources/sql/init.sql
```

### 2. 修改配置文件

- `application-dev.yml`：开发环境数据库/Redis/MinIO连接
- `application-prod.yml`：生产环境配置
- 默认启动环境：`dev`（在 `application.yml` → `spring.profiles.active`）

### 3. 构建 & 启动

```bash
# 编译
mvn clean compile

# 运行
mvn spring-boot:run

# 打包
mvn clean package -DskipTests
java -jar target/springboot-1.0.0-SNAPSHOT.jar
```

启动成功后访问：
- 接口文档 (Knife4j)：`http://localhost:8080/api/doc.html`
- 健康检查：`http://localhost:8080/api/health`
- API前缀：`/api`（`server.servlet.context-path`）

---

## 项目结构

```
src/main/java/com/example/springboot
├── Application.java              # Spring Boot 启动入口
├── common/                       # 通用层（切面/常量/异常/响应）
│   ├── annotation/
│   │   └── Auth.java             # 权限注解 @Auth(role="admin|user")
│   ├── constant/
│   │   ├── RoleConstant.java     # 角色常量（ADMIN / USER）
│   │   └── FileConstant.java     # 文件上传限制（大小/类型）
│   ├── exception/
│   │   ├── BusinessException.java    # 业务异常
│   │   ├── ErrorCode.java            # 错误码枚举
│   │   ├── GlobalExceptionHandler.java# 全局异常捕获
│   │   └── ThrowUtils.java           # 断言工具
│   ├── BaseResponse.java         # 统一响应包装
│   ├── DeleteRequest.java        # 批量删除DTO
│   ├── PageRequest.java          # 分页基类DTO
│   ├── ResultUtils.java          # 响应结果工具
│   └── UserContext.java          # 当前登录用户上下文（ThreadLocal）
├── config/                       # 配置类
│   ├── DataInitializer.java      # 启动时初始化默认管理员
│   ├── MinioConfig/Properties.java
│   ├── MybatisFlexConfig.java    # 分页/乐观锁插件
│   ├── RedisConfig.java          # Redis序列化
│   └── WebMvcConfig.java         # 注册拦截器/跨域
├── controller/                   # 控制层（路由定义 + 参数校验）
├── dto/                          # 数据传输对象（入参）
├── entity/                       # 数据库实体（与表一一对应）
├── interceptor/
│   └── AuthInterceptor.java      # 登录状态 / 权限校验拦截器
├── mapper/                       # MyBatis-Flex Mapper接口
├── service/                      # 业务逻辑层
│   └── impl/                     # 具体实现类
├── util/
│   ├── JsonUtil.java
│   └── RedisUtil.java            # Redis命令二次封装
└── vo/                           # 视图对象（接口出参）
```

---

## 核心架构设计

### ADR-001: 三层架构 + 依赖注入

- **Controller**：只做路由、参数校验、调用Service、包装响应。严禁包含业务逻辑。
- **Service**：承载核心业务，使用构造器注入依赖，便于单测 Mock。
- **Mapper**：MyBatis-Flex 基础Mapper，仅做数据库CRUD，复杂SQL放XML。
- **禁止反向依赖**：Controller → Service → Mapper，禁止 Mapper 调 Service、Service 调 Controller。

### ADR-002: 统一响应格式 `BaseResponse<T>`

所有接口返回统一结构：

```json
{
  "code": 0,
  "message": "ok",
  "data": {},
  "timestamp": 1721750400000
}
```

- `code = 0`：成功；`code != 0`：失败，客户端拦截并弹窗展示 `message`。
- 成功/失败分别由 `ResultUtils.success()` / `ResultUtils.error()` 构造。

### ADR-003: 权限体系

1. 登录态由 **Spring Session Redis** 存储，Session ID 写在 Cookie。
2. 自定义 `@Auth(role = RoleConstant.ADMIN)` 注解标记在 Controller 方法：
   - 写在类上：该类所有接口都需要该角色。
   - 写在方法上：覆盖类级设置。
3. `AuthInterceptor` 拦截器按顺序校验：
   - 是否登录 → 是否需要特定角色 → 通过/抛 401/403。
4. 当前用户ID写入 `UserContext`（ThreadLocal），Service层直接通过 `UserContext.getCurrentUserId()` 获取。

### ADR-004: 全局异常处理

`@RestControllerAdvice` 集中拦截三类异常：
- `BusinessException`：主动抛出的业务异常（如 "文章不存在"）
- `MethodArgumentNotValidException`：参数校验异常（如长度/必填校验）
- `Exception`：兜底捕获，打印日志并返回 500

> 严禁在 Controller 或 Service 中写 `try { ... } catch (Exception e) { e.printStackTrace(); }`，必须将异常抛出交给全局处理器。

---

## 数据库与持久层

### 表清单（`entity/`）

| 表名 / 实体 | 说明 |
|------------|------|
| `users` / `User` | 用户表（管理员、普通用户） |
| `articles` / `Articles` | 文章表（含slug、views、likes、SEO字段、封面） |
| `categories` / `Categories` | 分类表（slug 用于 SEO） |
| `tags` / `Tags` | 标签表 |
| `article_tag` / `ArticleTag` | 文章-标签 多对多关联表 |
| `comments` / `Comments` | 评论表，物化路径字段 `path` 实现级联删除 |
| `sys_config` / `SysConfig` | 系统配置KV（博客名、公告、ICP等） |
| `seo_configs` / `SeoConfigs` | 每页独立的 SEO Title/Keywords/Description |
| `daily_analytics` / `DailyAnalytics` | 每日 PV/UV 统计表 |
| `minio_item` / `MinioItem` | 上传到MinIO的文件元数据记录 |

### MyBatis-Flex 使用规范

1. **APT 自动生成表定义类**：编译后在 `target/generated-sources/annotations` 目录，形如 `ArticlesTableDef.ARTICLES`，所有SQL条件 **必须** 使用这些静态对象，禁止手写字符串列名。
2. **Lambda QueryWrapper 优先**：
   ```java
   List<Articles> list = list(QueryWrapper.create()
       .where(ARTICLES.STATUS.eq(1))
       .and(ARTICLES.CATEGORY_ID.eq(cid))
       .orderBy(ARTICLES.CREATED_AT, false));
   ```
3. **分页插件**：`MybatisFlexConfig` 中已注册 `PaginationInnerInterceptor`，使用 `page(Page.of(pageNum, pageSize), queryWrapper)` 直接分页。

---

## API 接口索引

所有接口已集成 Knife4j，运行后访问 **`/api/doc.html`** 可交互式调试。以下是接口模块分类总览：

### 1. 认证模块 `AuthController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| POST | `/auth/login` | ❌ | 账号密码登录，建立 Redis Session |
| POST | `/auth/logout` | ✅ 登录 | 退出登录，销毁 Session |
| GET | `/auth/me` | ✅ 登录 | 获取当前登录用户信息 |

### 2. 文章管理 `ArticleController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| POST | `/articles/page` | ❌ | 多维度筛选文章列表（支持状态/分类/标签/游标分页） |
| GET | `/articles/{idOrSlug}` | ❌ | 按 ID 或 URL别名 获取文章详情（PV+1，排行榜+分） |
| POST | `/articles` | ✅ 管理员 | 创建新文章（自动写入标签关联） |
| PUT | `/articles/{id}` | ✅ 管理员 | 编辑文章（增量更新 + 标签差量写入） |
| DELETE | `/articles/{id}` | ✅ 管理员 | 删除文章（级联清理标签/排行/推荐缓存） |
| POST | `/articles/{id}/like` | ❌（IP/Cookie限流） | 点赞（24小时一票，排行榜+5分） |

### 3. 分类管理 `CategoryController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| GET | `/categories` | ❌ | 全部分类列表（含文章数统计） |
| GET | `/categories/{id}` | ❌ | 分类详情 |
| POST | `/categories` | ✅ 管理员 | 新增分类 |
| PUT | `/categories/{id}` | ✅ 管理员 | 修改分类 |
| DELETE | `/categories/{id}` | ✅ 管理员 | 删除分类（空分类才允许删） |

### 4. 标签管理 `TagController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| GET | `/tags` | ❌ | 全部标签（云标签展示） |
| GET | `/tags/search` | ❌ | 关键字模糊搜索标签（创建文章时自动补全） |
| GET | `/tags/{id}/articles` | ❌ | 查询标签下所有关联文章（分页） |
| POST | `/tags` | ✅ 管理员 | 创建标签 |
| PUT | `/tags/{id}` | ✅ 管理员 | 修改标签（名称/颜色/Slug） |
| DELETE | `/tags/{id}` | ✅ 管理员 | 删除标签（不影响已发布文章） |

### 5. 评论管理 `CommentController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| GET | `/comments/article/{articleId}` | ❌ | 获取某篇文章评论（父子级树结构） |
| POST | `/comments` | ✅ 登录 | 发表评论（审核自动通过，排行榜+10分） |
| DELETE | `/comments/{id}` | ✅ 管理员 | 删除评论（物化路径级联删除所有子回复） |

### 6. 系统配置 `ConfigController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| GET | `/config/public` | ❌ | 获取前台公开配置（博客名/公告/备案号/SEO基础信息） |
| PUT | `/admin/config` | ✅ 管理员 | 修改系统配置（批量KV写入） |

### 7. SEO配置 `SeoConfigController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| GET | `/seo/{pageKey}` | ❌ | 按页面Key获取SEO配置（home/archive/category/tag等） |
| PUT | `/seo/{pageKey}` | ✅ 管理员 | 更新指定页面的SEO Title / Keywords / Description |

### 8. 访问统计 `DailyAnalyticsController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| GET | `/analytics/daily/trend` | ✅ 管理员 | 获取最近N天PV/UV折线图数据（默认30天） |
| GET | `/analytics/daily/overview` | ✅ 管理员 | 总览指标（累计PV/UV/今日/昨日环比） |

### 9. 文件上传 `FileController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| POST | `/upload/image` | ✅ 登录 | 上传图片（JPG/PNG/GIF/WEBP，单文件≤5MB） |
| POST | `/upload/cover` | ✅ 管理员 | 上传文章封面（JPG/PNG，单文件≤10MB，建议16:9） |
| POST | `/upload/avatar` | ✅ 登录 | 上传用户头像（JPG/PNG，单文件≤2MB，自动裁剪） |
| POST | `/upload/document` | ✅ 管理员 | 上传文档（PDF/MD/DOCX/ZIP，单文件≤100MB） |

> 上传接口由 `FileService` 做统一限制（`FileConstant`），防止违规文件类型上传至 MinIO。

### 10. 热门排行 + 个性化推荐 `TrendingController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| GET | `/articles/trending` | ❌ | 获取热门文章排行榜（周期：daily/weekly/monthly/all） |
| GET | `/articles/{id}/recommend` | ❌ | 获取「相关文章」个性化推荐（详情页侧边栏使用） |
| POST | `/articles/trending/rebuild` | ✅ 管理员 | 后台任务：重算所有文章热度并刷新 Redis 排行榜 |
| POST | `/articles/{id}/recommend/refresh` | ✅ 管理员 | 重新计算指定文章的相似推荐列表 |

### 11. 仪表盘 `DashboardController`

| 方法 | 路径 | 鉴权 | 说明 |
|-----|------|------|------|
| GET | `/admin/dashboard` | ✅ 管理员 | 后台首页概览（文章数/评论数/近7日趋势/热门Top5） |

---

## 核心业务模块实现细节

### 1. 高性能排行榜（Redis ZSET 实现）

**文件位置**：
- `service/TrendingService.java` / `service/impl/TrendingServiceImpl.java`
- `controller/TrendingController.java`
- `vo/TrendingVO.java`

#### Redis Key 设计

| Key 格式 | 数据结构 | 说明 |
|---------|---------|------|
| `article:trending:daily:yyyyMMdd` | ZSET | 日榜，每天自动换key，天然过期 |
| `article:trending:weekly:yyyyWW` | ZSET | 周榜 |
| `article:trending:monthly:yyyyMM` | ZSET | 月榜 |
| `article:trending:all` | ZSET | 总榜 |

ZSET 中 `member = articleId`，`score = 热度分值`。

#### 热度算法（实时更新 + 时间衰减）

```
原始分 = (浏览量 × 1) + (点赞数 × 5) + (评论数 × 10)
衰减因子 = 0.95 ^ 文章发布天数
最终分 = 原始分 × 衰减因子
```

#### 事件触发点

| 事件 | 触发位置 | 分值变化 |
|-----|---------|---------|
| 文章被阅读 | `ArticleServiceImpl.getArticleByIdOrSlug()` | +1 |
| 文章被点赞 | `ArticleServiceImpl.likeArticle()` | +5 |
| 新增审核通过评论 | `CommentServiceImpl.createComment()` | +10 |
| 新建/编辑/删除文章 | `ArticleServiceImpl` | 调用 `rebuildTrending()` 重算 |

### 2. 个性化文章推荐（基于内容的推荐算法）

**文件位置**：
- `service/RecommendService.java` / `service/impl/RecommendServiceImpl.java`
- `vo/RecommendVO.java`

#### 算法原理：多维度内容相似度加权

```
相似度总得分 =
  分类一致 (权重 40%) +
  标签Jaccard相似度 × 0.6 × 0.7 +
  标题/摘要关键词Jaccard相似度 × 0.6 × 0.3
```

**Jaccard 集合相似度**：
```
J(A, B) = |A ∩ B| / |A ∪ B|
```

#### 推荐理由生成规则

代码会根据命中的维度自动生成友好理由：
- `同分类「Java进阶」，包含相似标签: SpringBoot, Redis, 性能优化`
- `同分类「Java进阶」的相关文章`
- `包含相似标签: SpringBoot, Redis`
- `本文提及的高频词与您阅读的文章相似`

#### 缓存策略

- 首次计算后将 Top 10 的相似文章ID列表写入 Redis
- Key：`article:recommend:{articleId}`
- TTL：7 天
- 文章更新 / 删除时自动失效

### 3. 评论级联删除（物化路径法）

`Comments` 表中每条记录含有 `path` 字段，格式为 `父级路径/当前ID`，例如 `001/005/023`。

删除父评论时直接执行 SQL：
```sql
DELETE FROM comments WHERE path LIKE '父评论path%';
```
一条SQL即可删除整个子树，无需递归，性能稳定。

### 4. 文件上传类型安全

`FileConstant.java` 集中定义限制：
- 白名单扩展名集合 + MIME Type 双重校验
- 按上传场景（IMAGE/COVER/AVATAR/DOCUMENT）分别设定不同上限
- 上传后返回 MinIO 永久访问 URL，由 MinioConfig 自动拼接完整地址

---

## 配置参考

### `application.yml` 关键配置项

| 配置 | 默认值 | 说明 |
|-----|-------|------|
| `server.port` | 8080 | 服务端口 |
| `server.servlet.context-path` | `/api` | 统一API前缀 |
| `server.servlet.session.cookie.max-age` | 2592000 (30天) | 会话Cookie有效期（秒） |
| `spring.servlet.multipart.max-file-size` | 100MB | 单个文件上限 |
| `session.token-expiration` | 7200 | 访问Token过期（秒） |
| `session.refresh-token-expiration` | 604800 | 刷新Token过期（秒） |
| `minio.endpoint` | `http://...:9000` | MinIO API地址 |
| `minio.bucketName` | `xiaoxiao` | 存储桶名 |

---

## 代码规范速查

### 命名约定

| 分类 | 规则 | 示例 |
|-----|------|------|
| 表 | 全小写下划线，关联表用 `a_b` | `articles`, `article_tag` |
| 实体类 | 表名单数驼峰，首字母大写 | `Articles`, `ArticleTag` |
| Mapper | 实体名 + `Mapper` | `ArticlesMapper` |
| Service 接口 | 实体名（去掉s） + `Service` | `ArticleService` |
| Service 实现 | 接口名 + `Impl` | `ArticleServiceImpl` |
| DTO 入参 | `动作 + DTO` | `ArticleCreateDTO`, `ArticleQueryDTO` |
| VO 出参 | `展示含义 + VO` | `ArticleDetailVO`, `TrendingVO` |
| 常量类 | `XxxConstant`，常量全大写蛇形 | `RoleConstant.ADMIN` |

### 角色常量（禁止硬编码）

```java
// 正确
@Auth(role = RoleConstant.ADMIN)
// 错误
@Auth(role = "admin")
```

同理，所有硬编码的上传类型、错误消息均需抽到对应的 `*Constant` 或 `ErrorCode` 中。

### 业务异常抛出

```java
// Service 层，条件不满足时快速失败
ThrowUtils.throwIf(article == null, ErrorCode.NOT_FOUND_ERROR, "文章不存在");
// 或自定义
throw new BusinessException(ErrorCode.OPERATION_ERROR, "删除失败：该分类下仍存在文章");
```

---

## 常见问题

### Q1: 编译报错「找不到符号 类 ArticlesTableDef」

MyBatis-Flex 的 APT 表定义类在编译期才生成，请执行：
```bash
mvn clean compile
```
然后 IDEA 中将 `target/generated-sources/annotations` 标记为「Generated Sources Root」（右键 → Mark Directory as）。

### Q2: 接口返回 401 "未登录"

- 先调 `/auth/login`，成功后浏览器会自动写入 Cookie。
- Postman 需要将登录接口的 Cookie 复制到后续请求。
- 如果重启了Redis，Session会丢失，需要重新登录。

### Q3: 排行榜不更新 / 为空

- 确认 Redis 正常连接。
- 首次部署必须先调一次管理员接口 `POST /articles/trending/rebuild` 触发全量计算。

### Q4: MinIO 上传成功但访问 URL 403

检查 MinIO bucket 的 **访问策略**（Anonymous 或前缀规则），或者通过签名URL访问（FileService 后续可扩展签名URL模式）。

---

## License

本代码用于个人博客学习与生产，遵循项目主 LICENSE。